# running inference with pi0.5:
1. spin up the policy server in `openpi`
2. [run_inference.py](run_inference.py) to connect to websocket and query policy server

- need something to convert outputted actions into joint commands for xarm
- need an easier way to send trajectories to xarm
